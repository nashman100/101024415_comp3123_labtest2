import React from "react";

function Header() {
    return(
        <header className="text-center p-3 bg-primary text-white">
            <h1>Weather App</h1>
        </header>
    )
}

export default Header;